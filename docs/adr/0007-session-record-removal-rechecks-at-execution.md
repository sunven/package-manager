# 会话记录移除在动手时重新判断

确认框里的条数是确认当时的观察。每条会话记录在移入废纸篓的那一刻再判断一次：按年龄移除的，最后活动时间必须仍早于门槛，且未在使用中；单条移除的，它必须仍是会话记录，且未在使用中。最后活动时间更新不取消单条移除。不再符合的留下。结束后报告实际移入废纸篓的条数。

## Considered Options

按确认时冻结的名单移完。用户看到的数字和结果一致。否决是因为从确认到动手，对话可能被续写，文件也可能被代理进程打开。

## Consequences

- 确认条数可以大于实际移入条数。
- 不要为了让两个数字一致，改回按冻结名单移除。
