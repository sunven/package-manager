# 使用中的会话记录不移除

会话记录移除会把文件移入废纸篓，但代理 CLI 可能仍打开着该文件并继续写入。文件仍被其他进程打开的会话记录视为使用中，单条移除和按年龄批量移除都跳过它。

## Considered Options

一律允许移除。废纸篓可以恢复，实现也更简单。否决是因为移走后进程继续写原路径，进行中的对话会断掉，废纸篓里的副本也不完整。

## Consequences

- 使用中的记录仍出现在列表中，但不能成为移除对象。
- 进程关闭、文件不再被打开之后，同一条记录可以移除。
