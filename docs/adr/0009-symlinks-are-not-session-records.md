# 符号链接不是会话记录

会话记录必须是来源目录里的普通文件。符号链接不进入列表，也不能被移除。不跟随链接去移走它的目标。

## Considered Options

像 `codex-config` 那样解析真实路径，只要目标仍在来源目录内就移除目标。这样能清掉指向目录内部的别名，但也会把另一条会话记录，或链接外面的文件，当成这条目录项移走。

## Consequences

- 来源目录里的符号链接既不显示为会话记录，也不成为移除对象。
- 不要为了和 `codex-config` 的路径解析对齐，而跟随符号链接。
